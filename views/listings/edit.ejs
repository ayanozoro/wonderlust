<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>new listing</title>
</head>
<body>
    <h1>Edit Listing</h1>
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT" class="form-container">
        <div class="mb-3">
            <label for="title" class="form-label">Title:</label>
            <input type="text" name="listing[title]" id="title" value="<%= listing.title %>">
        </div>

        <div class="mb-3">
            <label for="description" class="form-label">Description:</label>
            <textarea name="listing[description]" id="description"><%= listing.description %></textarea>
        </div>

        <div class="mb-3">
            <label class="form-label">Images:</label>
            <div class="images-container">
                <div class="image-upload-box">
                    <input type="url" name="listing[image]" class="image-input" placeholder="Enter image URL">
                    <img src="<%= listing.image %>" alt="" class="preview-image">
                </div>
                <!-- <div class="image-upload-box">
                    <input type="url" name="listing[image2]" class="image-input" placeholder="Enter image URL">
                    <img src="" alt="" class="preview-image">
                </div>
                <div class="image-upload-box">
                    <input type="url" name="listing[image3]" class="image-input" placeholder="Enter image URL">
                    <img src="" alt="" class="preview-image">
                </div> -->
            </div>
        </div>

        <div class="mb-3">
            <label for="location" class="form-label">Location:</label>
            <input type="text" name="listing[location]" id="location" value="<%= listing.location %>">
        </div>

        <div class="mb-3">
            <label for="price" class="form-label">Price:</label>
            <input type="text" name="listing[price]" id="price" value="<%= listing.price %>">
        </div>

        <div class="mb-3">
            <label for="country" class="form-label">Country:</label>
            <input type="text" name="listing[country]" id="country" value="<%= listing.country %>">
        </div>

        <button class="submit-btn">Edit Listing</button>
    </form>

    <style>
        .images-container {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-bottom: 1rem;
        }

        .image-upload-box {
            flex: 1;
            min-width: 200px;
            max-width: 300px;
        }

        .image-input {
            width: 100%;
            margin-bottom: 0.5rem;
        }

        .preview-image {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 4px;
            border: 1px solid #ddd;
        }

        /* Your existing styles */
        .form-container {
            max-width: 900px;  /* Increased to accommodate images in a row */
            margin: 0 auto;
            padding: 20px;
        }

        .mb-3 {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input, textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .submit-btn {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .submit-btn:hover {
            background-color: #0056b3;
        }
    </style>

    <script>
        const imageInputs = document.querySelectorAll('.image-input');
        imageInputs.forEach(input => {
            input.addEventListener('change', function() {
                const previewImage = this.nextElementSibling;
                previewImage.src = this.value;
            });
        });
    </script>
</body>
</html>